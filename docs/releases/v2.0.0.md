# Release Checklist - MAPLE v2.0.0

<!-- G6 candidate artifact. External publication remains human-gated. -->

**Status:** LOCAL RELEASE CANDIDATE / NOT PUBLISHED
**Target:** `2.0.0`
**Date opened:** 2026-08-29

## Release rationale

MAPLE 2.0.0 promotes the accumulated autonomous-runtime, interoperability,
security, reliability, retrieval, and task-management surface to the next
major package version. This release also reconciles the checked-in CI contract:
all strict Flake8 findings are fixed in source, all MAPLE modules carry the
existing author notice, and no lint or license policy was weakened or changed.

The release marker is a major-version promotion. The version change itself
does not claim that hosted coordination, managed stores, sandboxed execution,
or external registry services are included; those remain explicit follow-up
contracts in the parity plan.

## Entry gates

- [x] `VERSION`, `maple.__version__`, test metadata, README current markers, and
  changelog agree on `2.0.0`.
- [x] ADR-144 records the approved source-normalization decision.
- [x] Slice 200 brief and plan record the `610` line-length / `20` header
  baseline and the zero-finding result.
- [ ] Project Reviewer sign-off recorded for the exact release commit.
- [ ] Release commit is on the intended protected branch with a clean tree.

## Local quality gates

- [x] Black check passes for `maple/`.
- [x] isort check passes for `maple/`.
- [x] Exact workflow Flake8 command passes with zero findings.
- [x] All `103` MAPLE Python modules pass the workflow-equivalent author scan.
- [x] Python compilation passes for `maple/`.
- [ ] Whole-package mypy and Ruff rerun on the exact release commit.
- [ ] Bandit and declared-project pip-audit rerun on the exact release commit.
- [ ] Gitleaks result recorded, or an explicit tooling limitation recorded.
- [ ] Full test suite rerun on the exact release commit.

## Package gates

- [ ] Clean exact-commit wheel and sdist built with `python -m build`.
- [ ] Twine metadata checks pass for both artifacts.
- [ ] Isolated no-dependency install imports MAPLE and reports `2.0.0`.
- [ ] Offline doctor reports `ready=true`, `status=SUCCESS`, and `network=false`.
- [ ] Sdist audit confirms the intended core distribution boundary.

## Deferred parity work

The following are planned, not silently represented as complete in 2.0.0:

- hosted identity, tenancy, policy, and managed control-plane services;
- distributed scheduling, leases, federation, and exactly-once side effects;
- managed state/vector stores and provider-owned retrieval services;
- sandboxed code execution, browser/computer control, and isolation policy;
- hosted observability/evals, language breadth, and production hosting.

See [the parity follow-up plan](../plans/maple-agent-framework-parity-followups.md).

## External ship gates - human action required

- [ ] Cloud provider selected and recorded before any cloud SDK/deploy call.
- [ ] Website target/domain and deployment authorization recorded.
- [ ] Registry targets selected: Test PyPI, PyPI, GitHub release, or another
  approved registry.
- [ ] Human explicitly authorizes creating/pushing tag `v2.0.0`.
- [ ] Human explicitly authorizes external publication.
- [ ] Release environment approval and artifact provenance recorded.
- [ ] Published install and quickstart smoke tests pass.

No tag, publication, registry write, cloud action, or website deployment is
claimed by this checklist.
